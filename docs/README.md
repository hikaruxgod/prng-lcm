# Лабораторная работа: Генераторы псевдослучайных чисел (ГПСЧ)

## Цель работы

Реализация собственного ГПСЧ на основе линейного конгруэнтного метода, сравнение с библиотечным генератором, тестирование обоих генераторов на равномерность и независимость.

---

## Реализация

### 1. Собственный ГПСЧ (`rundum`)

**Метод:** Мультипликативный линейный конгруэнтный генератор (ЛКМ)

**Формула:**
X(n+1) = (a × X(n)) mod m


**Параметры:**
- **a** (множитель) = 1220703125
- **c** (приращение) = 0 (мультипликативный вариант)
- **m** (модуль) = 2³¹ = 2147483648
- **Зерно (seed):** `time(nullptr)` — текущее время в секундах

**Нормализация:**
Результат делится на 2³¹ для получения значений в диапазоне [0, 1):
r[i] = static_cast<float>(b) * 4.6566128730773926e-10f

**Особенности реализации:**
- Функция возвращает `std::vector<float>` для автоматического управления памятью
- `static long b` сохраняет состояние между вызовами

---

### 2. Библиотечный генератор

**Метод:** Mersenne Twister (MT19937)

**Характеристики:**
- **Период:** 2¹⁹⁹³⁷ - 1
- **Распределение:** Равномерное [0, 1)
- **Источник энтропии:** `std::random_device` (случайное зерно из ОС)

**Реализация:**
std::random_device rd;
std::mt19937 gen(rd());
std::uniform_real_distribution<float> dis(0.0f, 1.0f);

---

## Тестирование

### Тест 1: Колмогорова-Смирнова (проверка равномерности)

**Что проверяет:** Соответствует ли эмпирическое распределение теоретическому равномерному.

**Метод:**
1. Сортируем выборку из 1000 чисел
2. Вычисляем максимальное отклонение между эмпирической функцией распределения F_emp и теоретической F_theor
3. Сравниваем со критическим значением: `D_крит = 1.36 / √n`

**Критерий:**
- Если **D < D_крит** → распределение равномерное
- Если **D ≥ D_крит** → распределение НЕ равномерное

---

### Тест 2: Автокорреляционный (проверка независимости)

**Что проверяет:** Зависят ли соседние числа друг от друга.

**Метод:**

Вычисляем коэффициент автокорреляции с лагом 1:
ρ₁ = Σ(X(i) - μ)(X(i+1) - μ) / Σ(X(i) - μ)²

**Критерий:**
- Если **|ρ₁| < 0.1** → числа независимы
- Если **|ρ₁| ≥ 0.1** → есть автокорреляция

---

## Результаты тестирования

### Демонстрация генерации (10 чисел)

**rundum:**
0.948457, 0.686809, 0.218761, 0.131432, 0.452031,
0.689306, 0.699528, 0.842409, 0.942396, 0.465555

**std::mt19937:**
0.0894493, 0.113074, 0.172176, 0.168034, 0.251292,
0.255039, 0.815873, 0.533108, 0.107223, 0.123893

---

### Тест Колмогорова-Смирнова (n = 1000)

| Генератор | Статистика D | Критическое значение | Результат |
|-----------|--------------|---------------------|-----------|
| **rundum** | 0.0220 | 0.0430 | ✅ **Равномерное распределение** |
| **std::mt19937** | 0.0406 | 0.0430 | ✅ **Равномерное распределение** |

**Анализ:**
- Оба генератора прошли тест (D < 0.043)
- `rundum` показал отличный результат (D = 0.022)
- `std::mt19937` близок к границе, но укладывается в норму

---

### Автокорреляционный тест (n = 1000)

| Генератор | Коэффициент ρ₁ | Порог | Результат |
|-----------|----------------|-------|-----------|
| **rundum** | -0.0054 | 0.1 | ✅ **Нет автокорреляции** |
| **std::mt19937** | -0.0593 | 0.1 | ✅ **Нет автокорреляции** |

**Анализ:**
- Оба генератора показывают независимость (|ρ₁| < 0.1)
- Отрицательные значения близки к нулю — нормальное явление
- Числа не имеют линейной зависимости между соседними элементами

---

## Выводы

### Успешные результаты:

1. **rundum (мультипликативный ЛКМ):**
   - Равномерное распределение (K-S тест пройден)
   - Независимость чисел (автокорреляция отсутствует)
   - Простота реализации и высокая скорость
   - Период = 2³¹ (меньше, чем у MT19937)

2. **std::mt19937 (Mersenne Twister):**
   - Равномерное распределение (K-S тест пройден)
   - Независимость чисел (автокорреляция отсутствует)
   - Огромный период (2¹⁹⁹³⁷ - 1)
   - Отраслевой стандарт для некриптографических задач

---

### Сравнение генераторов:

| Критерий | rundum (ЛКМ) | std::mt19937 |
|----------|--------------|--------------|
| **Равномерность** | Отлично (D=0.022) | Хорошо (D=0.041) |
| **Независимость** | Отлично (ρ=-0.005) | Хорошо (ρ=-0.059) |
| **Период** | 2³¹ ≈ 2.1×10⁹ | 2¹⁹⁹³⁷-1 (огромен) |
| **Скорость** | Очень быстрый | Быстрый |
| **Предсказуемость** | Легко восстановить параметры | Сложнее |
| **Применение** | Игры, симуляции | Везде, кроме криптографии |

---

## Компиляция и запуск

### Требования:
- Компилятор с поддержкой C++11 или выше
- Стандартная библиотека C++

### Команда компиляции:
```bash
g++ -std=c++11 -o prng_test main.cpp
```

### Запуск:
```bash
./prng_test
```

---

## Структура проекта

prng-lcm/
├── src/
│   └── main.cpp              # Основной код программы
├── docs/
│   ├── README.md             # Документация (этот файл)
│   └── theory.txt            # Теоретическая справка для защиты
├── results/
│   └── output.txt            # Результаты выполнения программы
├── .gitignore                # Игнорируемые файлы

---

## Теоретическая справка

### Линейный конгруэнтный метод (ЛКМ)

**Общая формула:**
X(n+1) = (a × X(n) + c) mod m

**Мультипликативный вариант (c = 0):**
X(n+1) = (a × X(n)) mod m

**Условия для хорошего ЛКМ:**
- m должно быть большим (желательно степень двойки)
- a должно быть подобрано для максимального периода
- Зерно X₀ не должно быть 0 (для мультипликативного)

---

### Тест Колмогорова-Смирнова

**Статистика:**
D = max|F_emp(x) - F_theor(x)|

**Критическое значение (α = 0.05):**
D_крит = 1.36 / √n

Где α = 0.05 означает уровень значимости 5% (95% доверительный интервал).

---

### Автокорреляция

**Коэффициент с лагом k:**
ρ_k = Σ(X(i) - μ)(X(i+k) - μ) / Σ(X(i) - μ)²


**Интерпретация:**
- ρ ≈ 0: нет корреляции (независимость)
- ρ > 0: положительная корреляция
- ρ < 0: отрицательная корреляция

---

## Полный вывод программы
Числа из функции rundum:
0.948457
0.686809
0.218761
0.131432
0.452031
0.689306
0.699528
0.842409
0.942396
0.465555

Числа из стандартной библиотеки:
0.0894493
0.113074
0.172176
0.168034
0.251292
0.255039
0.815873
0.533108
0.107223
0.123893

Тест Колмогорова-Смирнова для rundum:
Статистика D: 0.0219934
Критическое значение (0.05): 0.043007
Распределение равномерно

Тест Колмогорова-Смирнова для стандартного генератора:
Статистика D: 0.0405904
Критическое значение (0.05): 0.043007
Распределение равномерно

Тесты независимости (автокорреляционный тест):

Автокорреляционный тест для rundum:
Коэффициент автокорреляции: -0.00536186
Нет значимой автокорреляции

Автокорреляционный тест для стандартного генератора:
Коэффициент автокорреляции: -0.059297
Нет значимой автокорреляции

---

## 👤 Автор

**Локтеев Илья Дмитриевич**
- Группа: ПМИ-122
- GitHub: https://github.com/hikaruxgod
